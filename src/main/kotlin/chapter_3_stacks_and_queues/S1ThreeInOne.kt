package chapter_3_stacks_and_queues

class S1ThreeInOne {
    /*
        In order to solve this, we can assume that the array is divided to 3 equal sections
        Each section will have its own pointer (p1, p2, p3)
        Each pointer will represent the top of the stack
        Representation of empty stack: p is in the beginning of section + value is null
     */
}